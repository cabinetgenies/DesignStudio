import { FolderIcon } from "@/components/icons";
import PagePlaceholder from "@/components/PagePlaceholder";

export default function ProjectsPage() {
  return (
    <PagePlaceholder
      title="Projects"
      description="Browse, open, and manage your client visualization projects."
      icon={FolderIcon}
    />
  );
}
